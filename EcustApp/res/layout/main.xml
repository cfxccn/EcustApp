<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent" >

    <FrameLayout
        android:id="@+id/fragment_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/tabs_rg" />
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#000000" 
        android:layout_above="@+id/tabs_rg" />
    <RadioGroup
        android:id="@+id/tabs_rg"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:gravity="center"
        android:orientation="horizontal"
        android:paddingBottom="7dp"
        android:paddingTop="7dp" >

        <RadioButton
            android:id="@+id/tab_rb_a"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1.0"
            android:button="@null"
            android:drawableTop="@drawable/home_tab_home"
            android:gravity="center"
            android:singleLine="true"
            android:text="首页" />

        <RadioButton
            android:id="@+id/tab_rb_b"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1.0"
            android:button="@null"
            android:drawableTop="@drawable/home_tab_nearby"
            android:gravity="center"
            android:singleLine="true" 
            android:text="附近" />


        <RadioButton
            android:id="@+id/tab_rb_c"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1.0"
            android:button="@null"
            android:drawableTop="@drawable/home_tab_post"
            android:gravity="center"
            android:singleLine="true" 
            android:text="广场" />


        <RadioButton
            android:id="@+id/tab_rb_d"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1.0"
            android:button="@null"
            android:drawableTop="@drawable/home_tab_setting"
            android:gravity="center"
            android:singleLine="true"
            android:text="设置" />

    </RadioGroup>

</RelativeLayout>