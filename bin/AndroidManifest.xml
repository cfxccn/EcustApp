<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.usta"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="19" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.usta.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.usta.SearchDrugs"
             android:label="药品查询"></activity>
        <activity android:name="com.usta.SearchBooks" 
            android:label="图书查询" ></activity>
        <activity android:name="com.usta.SearchClassroom"
             android:label="教室查询"></activity>
        <activity android:name="com.usta.Map"
            android:label="教室查询"></activity>   
            <activity android:name="com.usta.Joke"
                android:label="每日笑话"></activity>   
            <activity android:name="com.usta.Linknet"
                 android:label="链接校园网"></activity>   
    </application>

</manifest>
